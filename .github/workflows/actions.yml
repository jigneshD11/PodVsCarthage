name: Test

on:
  pull_request:
    branches:
    - master

jobs:
  build:

    runs-on: macos

    steps:
      - uses: actions/checkout@v1
      - run: brew install fastlane
      - run: pod install
      - run: fastlane testLane
